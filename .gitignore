# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Editor / OS
.idea/
.vscode/
.DS_Store

# Claude Code local state (machine-local settings + agent worktrees)
.claude/

# Jupyter
.ipynb_checkpoints/

# Big artefacts that shouldn't be in git
checkpoints/
*.h5
*.pt
*.bin

# Prediction outputs: track real experiments in git so the repo is
# explorable without re-running, but skip noisy or in-progress artefacts.
predictions/my_model/
predictions/*/*.partial.json

# Personal notes / specs (never committed)
.specs/

# LatinPipe vendored repo's own venv (we recreate per machine)
third_party/latinpipe/.venv/

# Downloaded UD treebanks (raw few-shot example source; regenerable via builder)
data/ud_treebanks/
